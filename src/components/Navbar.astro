---
import ThemeToggle from './ThemeToggle.jsx';

const links = [
  { href: '#home', label: 'Home' },
  { href: '#about', label: 'About' },
  { href: '#skills', label: 'Skills' },
  { href: '#projects', label: 'Projects' },
  { href: '#contact', label: 'Contact' },
];
---

<nav id="site-nav" class="sticky top-0 z-50 bg-surface/80 dark:bg-surfaceDark/80 backdrop-blur-xl transition-shadow duration-300">
  <div class="max-w-[1200px] mx-auto flex items-center justify-between px-6 md:px-10 py-5">

    <a href="#home" class="text-[17px] font-bold tracking-tight">
      Sugumaran<span class="text-accent dark:text-accentDark font-bold"> S</span>
    </a>

    <ul class="hidden md:flex items-center gap-8 list-none">
      {links.map((link) => (
        <li>
          <a
            href={link.href}
            class="text-sm text-inkMuted dark:text-white/70 hover:text-ink dark:hover:text-white transition-all duration-150 hover:scale-105 inline-block"
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>

    <div class="flex items-center gap-3">
      <ThemeToggle client:load />

      <button
        id="mobile-menu-btn"
        aria-label="Toggle menu"
        aria-expanded="false"
        class="md:hidden flex items-center justify-center w-9 h-9 rounded-lg text-ink dark:text-white hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
      >
        <svg id="icon-hamburger" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="w-5 h-5">
          <path d="M3 6h18M3 12h18M3 18h18" stroke-linecap="round" />
        </svg>
        <svg id="icon-close" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="w-5 h-5 hidden">
          <path d="M18 6L6 18M6 6l12 12" stroke-linecap="round" />
        </svg>
      </button>
    </div>
  </div>

  <ul
    id="mobile-menu"
    class="hidden md:hidden flex-col list-none border-t border-borderLight dark:border-borderDark px-6 py-3"
  >
    {links.map((link) => (
      <li>
        <a
          href={link.href}
          class="mobile-menu-link block py-3 text-base text-inkMuted dark:text-white/70 hover:text-ink dark:hover:text-white transition-colors"
        >
          {link.label}
        </a>
      </li>
    ))}
  </ul>
</nav>

<script>
  // Mobile menu toggle
  const menuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const iconHamburger = document.getElementById('icon-hamburger');
  const iconClose = document.getElementById('icon-close');

  function closeMenu() {
    mobileMenu?.classList.add('hidden');
    mobileMenu?.classList.remove('flex');
    iconHamburger?.classList.remove('hidden');
    iconClose?.classList.add('hidden');
    menuBtn?.setAttribute('aria-expanded', 'false');
  }

  function toggleMenu() {
    const isOpen = !mobileMenu?.classList.contains('hidden');
    if (isOpen) {
      closeMenu();
    } else {
      mobileMenu?.classList.remove('hidden');
      mobileMenu?.classList.add('flex');
      iconHamburger?.classList.add('hidden');
      iconClose?.classList.remove('hidden');
      menuBtn?.setAttribute('aria-expanded', 'true');
    }
  }

  menuBtn?.addEventListener('click', toggleMenu);
  document.querySelectorAll('.mobile-menu-link').forEach((link) => {
    link.addEventListener('click', closeMenu);
  });

  // Add shadow on scroll (replaces border-b)
  const nav = document.getElementById('site-nav');
  window.addEventListener('scroll', () => {
    if (window.scrollY > 10) {
      nav?.classList.add('shadow-[0_1px_0_0_rgba(0,0,0,0.06),0_4px_16px_-4px_rgba(0,0,0,0.08)]');
      nav?.classList.add('dark:shadow-[0_1px_0_0_rgba(255,255,255,0.06)]');
    } else {
      nav?.classList.remove('shadow-[0_1px_0_0_rgba(0,0,0,0.06),0_4px_16px_-4px_rgba(0,0,0,0.08)]');
      nav?.classList.remove('dark:shadow-[0_1px_0_0_rgba(255,255,255,0.06)]');
    }
  }, { passive: true });
</script>
