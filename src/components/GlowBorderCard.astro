---
interface Props {
  class?: string;
  delay?: number;
}

const { class: className = '', delay = 0 } = Astro.props;
---

<div class={`card-surface reveal ${className}`} style={`transition-delay: ${delay}s`}>
  <slot />
</div>
